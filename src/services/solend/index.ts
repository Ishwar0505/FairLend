/**
 * Save Finance (Solend) service wrapper.
 * Integrates with @solendprotocol/solend-sdk.
 *
 * TODO (Phase 2):
 * - Initialize SolendMarket
 * - Fetch reserves and normalize to UnifiedMarket
 * - Fetch obligations and normalize to UnifiedPosition
 * - Build deposit/borrow/withdraw/repay transactions
 */

export const SolendService = {
  // Placeholder — implemented in Phase 2
};
