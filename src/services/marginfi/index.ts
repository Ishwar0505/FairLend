/**
 * marginfi (mrgnlend) service wrapper.
 * Integrates with @mrgnlabs/marginfi-client-v2.
 *
 * TODO (Phase 2):
 * - Initialize MarginfiClient
 * - Fetch banks and normalize to UnifiedMarket
 * - Fetch accounts and normalize to UnifiedPosition
 * - Build deposit/borrow/withdraw/repay transactions
 */

export const MarginfiService = {
  // Placeholder — implemented in Phase 2
};
